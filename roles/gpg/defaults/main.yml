---
gpg_config_directory: '{{ ansible_env.HOME }}/.gnupg'
gpg_private_keys: {}
gpg_public_keys: {}
gpg_required_packages:
  macosx:
    - libgpg-error
    - pinentry
    - libgcrypt
    - libusb
    - gpg2
    - gpg-agent
  fedora:
    - gnupg
  ubuntu:
    - gnupg
